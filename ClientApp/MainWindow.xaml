<Window x:Class="ClientApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientApp"
        mc:Ignorable="d"
        Title="StudentWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Student Details" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Name:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedStudent.name}" Grid.Row="0" Margin="60,0,0,0"/>

            <TextBlock Text="Surname:" FontWeight="Bold" Grid.Row="1" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding SelectedStudent.surname}" Grid.Row="1" Margin="60,10,0,0"/>
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{Binding OtherStudents}" Margin="10" BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding name}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding surname}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding grade}" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
                <!-- кнопка изменить информацию самого студента-->
                <!--.............................................-->
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
